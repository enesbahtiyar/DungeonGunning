public enum StatType
{
    Health,
    AttackPower,
    FireRate,
    MoveSpeed,
    CooldownModifier,
    AttackRange
}

public enum ModifierType
{
    Flat,
    Percent
}

public enum EntityType
{
    AI,
    Player
}